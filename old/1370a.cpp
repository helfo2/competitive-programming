#include <iostream>
#include <algorithm>
#include <cmath>
#define ll unsigned long long
#define IOS ios::sync_with_stdio(0); cin.tie(0); cout.tie(0);
using namespace std;

int main(void){
    IOS;

    int t; cin >> t;
    while(t--){
        ll n; cin >> n;
        ll _max = 0;

        cout << n / 2 << endl;
    }

    return 0;
}